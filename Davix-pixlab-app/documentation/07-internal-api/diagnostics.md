# Diagnostics

- `/internal/admin/diagnostics/request-log` (GET): returns DB time, schema presence for `request_log` and `usage_monthly`, create SQL, and a sample insert test.
- **Auth**: Bridge token required.
- **Use**: Health checks for logging infrastructure.
