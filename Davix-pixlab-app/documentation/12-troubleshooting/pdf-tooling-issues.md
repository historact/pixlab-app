# PDF Tooling Issues

- `pdf_tool_failed` may indicate missing `pdftoppm` binary or invalid PDF files.
- `invalid_parameter` when `action` is unsupported.
- Large PDFs for public keys may hit size/file limits; reduce upload size or use owner/customer key.
