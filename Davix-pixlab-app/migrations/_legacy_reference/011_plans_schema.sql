CREATE TABLE IF NOT EXISTS plans (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  plan_slug VARCHAR(190) NOT NULL,
  name VARCHAR(255) NULL,
  billing_period VARCHAR(64) NULL,
  monthly_quota_files INT NULL,
  max_files_per_request INT NULL,
  max_total_upload_mb INT NULL,
  max_dimension_px INT NULL,
  timeout_seconds INT NULL,
  allow_h2i TINYINT(1) NULL DEFAULT 1,
  allow_image TINYINT(1) NULL DEFAULT 1,
  allow_pdf TINYINT(1) NULL DEFAULT 1,
  allow_tools TINYINT(1) NULL DEFAULT 1,
  is_free TINYINT(1) NULL DEFAULT 0,
  description TEXT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uniq_plan_slug (plan_slug)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

ALTER TABLE plans
  ADD COLUMN IF NOT EXISTS plan_slug VARCHAR(190) NOT NULL AFTER id,
  ADD COLUMN IF NOT EXISTS name VARCHAR(255) NULL AFTER plan_slug,
  ADD COLUMN IF NOT EXISTS billing_period VARCHAR(64) NULL AFTER name,
  ADD COLUMN IF NOT EXISTS monthly_quota_files INT NULL AFTER billing_period,
  ADD COLUMN IF NOT EXISTS max_files_per_request INT NULL AFTER monthly_quota_files,
  ADD COLUMN IF NOT EXISTS max_total_upload_mb INT NULL AFTER max_files_per_request,
  ADD COLUMN IF NOT EXISTS max_dimension_px INT NULL AFTER max_total_upload_mb,
  ADD COLUMN IF NOT EXISTS timeout_seconds INT NULL AFTER max_dimension_px,
  ADD COLUMN IF NOT EXISTS allow_h2i TINYINT(1) NULL DEFAULT 1 AFTER timeout_seconds,
  ADD COLUMN IF NOT EXISTS allow_image TINYINT(1) NULL DEFAULT 1 AFTER allow_h2i,
  ADD COLUMN IF NOT EXISTS allow_pdf TINYINT(1) NULL DEFAULT 1 AFTER allow_image,
  ADD COLUMN IF NOT EXISTS allow_tools TINYINT(1) NULL DEFAULT 1 AFTER allow_pdf,
  ADD COLUMN IF NOT EXISTS is_free TINYINT(1) NULL DEFAULT 0 AFTER allow_tools,
  ADD COLUMN IF NOT EXISTS description TEXT NULL AFTER is_free,
  ADD COLUMN IF NOT EXISTS created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER description,
  ADD COLUMN IF NOT EXISTS updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP AFTER created_at,
  ADD UNIQUE KEY uniq_plan_slug (plan_slug);
